# Copy to .env and fill in the keys for the providers you plan to use.
# You only need one — the service lazily instantiates providers on first use.

ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
